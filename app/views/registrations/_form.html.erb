<%= form_with(model: [@event,@registration], local: true ) do |form| %>
  <%= render "shared/errors", object: @registration %>
               
                <%= form.label :how_heard%>
                 <%= form.select :how_heard,Registration::HOW_HEARD_OPTIONS,prompt: "picked one"%>
                
    
    <%= form.submit %>
  
<% end %>